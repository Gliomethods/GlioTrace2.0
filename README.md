# GlioTrace
# Installation

Clone the repository, then install from the repository root:

`pip install .`

The package can now be used under name of *gliotrace*. Checkout the Example in the root directory for correct function call